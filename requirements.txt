# email_routine.py only uses Python stdlib (imaplib, smtplib, email, urllib).
# Pin nothing here yet; this file exists so the workflow's `pip install` step is a no-op
# until additional libraries are needed.
